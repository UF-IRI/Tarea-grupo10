#include "example.h"
bool EsPar(int num)
{
    if (num % 2 == 0)
        return true;
    else
        return false;
}

int Maximo(int num1, int num2)
{
    if (num1 > num2)
        return num1;
    else return num2;
}